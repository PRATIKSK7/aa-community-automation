# Set to 'true' to run tests against the local mock server (bypasses real credentials)
USE_MOCK_API=false

AA_USERNAME=your_username
AA_PASSWORD=your_password
AA_BASE_URL=https://example.com
AA_API_TOKEN=your_api_token
